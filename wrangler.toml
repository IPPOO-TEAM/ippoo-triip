# Cloudflare Pages / Workers — configuration projet.
name = "ippoo-triip"
compatibility_date = "2026-09-02"

# Dossier de sortie Vite — utilisé par Workers Builds et `wrangler deploy`.
pages_build_output_dir = "dist"

[assets]
directory = "./dist"
